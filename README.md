# corpus-webhook-test

Tiny static site used to exercise corpusAI's push-to-deploy webhook.
busybox httpd on port 8080; builds in seconds. Bump the version line in
`index.html` and push to trigger a deploy.
